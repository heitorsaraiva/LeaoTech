/*
-----------IF TERNÁRIO----------
    let erro = false
    let logado = true
    // (condição) ? 'valor se verdade' : 'Valor se for falso' 
    let log = (erro == true) ? 'Deu erro no sistema':'O sistema está funcionando'
    log = (logado = true) ? (erro = true) ? 'logado com erro':'logado sem erro':'usuario n esta ligado'
    console.log(log)
*/

/*
 -----------SWITCH---------------
 let dia = parseInt(prompt('Digite o dia:'))
 switch (dia) {
     case 1: 
     console.log('Domingo')
     break;

     case 2: 
     console.log('Segunda')
     break;
     
     case 3: 
     console.log('Terça')
     break;

     case 4: 
     console.log('Quarta')
     break;

     case 5: 
     console.log('Quinta')
     break;

     case 6: 
     console.log('Sexta')
     break;

     case 7: 
     console.log('Sábado')
     break;

     default: 
     console.log('Entrada errada.')
     break;
 }
 */


/*
-----------Laços de Repetição(LOOP)---------------
-----------for---------------
    for ->  sabe quantas vezes o código tem que executar
    for (inicializa um contador ; condição ; incremento){
            Bloco de código
        }
    let numero = parseInt(prompt(`Digite o valor`))
    for (let i = 0; i < 10; i++) {
        console.log(`${numero} x ${i+1} = ${numero *  (i+1)}`)

    }

-----------While(ENQUANTO)---------------
    while (condição) {
    coódigo
    }

    let i = 0
    while(i < 10) {

    }

-----------Do(FAÇA: EXECUTA E PERGUNTA DPS) While---------------
    let = i = 10
    do(i < 10) {
    console.log(i++)
    } while(i < 10)

    }

 */